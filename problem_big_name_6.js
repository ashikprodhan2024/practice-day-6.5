var friends = ["rahim", "karim", "abdul", "sadsd", "heroAlom"];

for(let i = 0; i < friends.length; i++){
    max_name=[0]
    let ans = friends[i];
    if(max_name.length < ans.length)
    {
        max_name = ans;

    }
}
console.log(max_name);